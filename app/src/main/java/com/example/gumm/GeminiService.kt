package com.example.gumm

// Gemini / network layer removed — Gumm runs 100% on-device.
// This file is intentionally left as a stub so the build doesn't break
// if any old reference accidentally remains.
